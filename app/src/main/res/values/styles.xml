<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>


    </style>

    <style name="EditTextTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:colorControlNormal">@color/colorGreyGravel</item>
        <item name="android:colorControlActivated">@color/colorBrandOrange</item>
    </style>

    <style name="StyledEditText">
        <item name="android:theme">@style/EditTextTheme</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="colorControlNormal">@color/colorBrandRed</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar">
        <item name="windowActionBar">true</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light"/>
    
    <!-- Styles for reports -->
    <style name="def_text_view">
        <item name="android:textAlignment">center</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_width">match_parent</item>
    </style>
    <style name="style_for_lable" parent="def_text_view">
        <item name="android:textSize">15dp</item>
    </style>
    <style name="style_for_value" parent="def_text_view">
        <item name="android:textSize">16dp</item>
        <item name="android:textColor">#000000</item>
    </style>

    <style name="table_header" parent="style_for_lable">
        <item name="android:layout_weight">1</item>
    </style>
    <style name="table_row" parent="style_for_value">
        <item name="android:layout_weight">1</item>
    </style>
</resources>
